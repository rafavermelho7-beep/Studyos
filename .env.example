# Local dev (SQLite). To migrate to Postgres/Supabase, change the provider
# in prisma/schema.prisma and point this at your Postgres connection string.
DATABASE_URL="file:./dev.db"

# Generate with: npx auth secret
AUTH_SECRET="replace-me"
